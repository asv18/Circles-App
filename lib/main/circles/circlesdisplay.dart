import 'package:flutter/material.dart';

class CirclesDisp extends StatelessWidget {
  const CirclesDisp({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Circles"),
      ),
    );
  }
}
