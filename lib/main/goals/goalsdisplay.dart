import 'package:flutter/material.dart';

class GoalsDisp extends StatelessWidget {
  const GoalsDisp({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Goals"),
      ),
    );
  }
}
